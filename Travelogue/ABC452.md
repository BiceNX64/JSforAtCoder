



**Scroll down for English**

# 📝 ABC452振り返り(2026-04-04)

2週間ぶりの参加です。とうとうレーティングが低下してしまいました。体感でも今までで一番できなかったです。2週間以上JSも競プロも全くやっていなかったのでしょうがないです。

| **順位** | **ユーザ** | **得点**          | **A**                                                        | **B**                                                        | **C** | **D** | **E** | **F** | **G** |
| -------- | ---------- | ----------------- | ------------------------------------------------------------ | ------------------------------------------------------------ | ----- | ----- | ----- | ----- | ----- |
| 10009    | **Bice**   | ** 300** (6)76:26 | [**100**](https://atcoder.jp/contests/abc452/submissions/74663254) (4)25:22 | [**200**](https://atcoder.jp/contests/abc452/submissions/74673023) (2)46:26 | (6)   | -     | -     | -     | -     |

Aで4回、Bで2回お手つきをしました。問題を読み違えたり、単純な条件の漏れがあったり、余計な出力をしたり、求められた回答の形式ではなかったり、いろいろと酷かったです。

## 🏆 コンテスト成績

| **項目**       | **内容**                                                     |
| -------------- | ------------------------------------------------------------ |
| ユーザ名       | [**Bice**](https://atcoder.jp/users/Bice)                    |
| コンテスト名   | [AtCoder Beginner Contest 452](https://atcoder.jp/contests/abc452) |
| 順位           | [10009th](https://atcoder.jp/contests/abc452/standings?watching=Bice) / 12501 |
| パフォーマンス | 129                                                          |
| レーティング   | **232** → **219** (-13)                                      |
| **段級位**     | **10 級**                                                    |

## 📈 レーティングについて

言うことはないです。初参加のパフォーマンスが134なので最低を更新しました。

A、B、Cの問題をGeminiのアドバイスを参考にレビューしていきます(問題文の掲載は著作権等の関係で控えさせていただきます。Linkから確認してください)。

## 🔠 [ABC452-A](https://atcoder.jp/contests/abc452/tasks/abc452_a)

```javascript
// ABC452
// https://atcoder.jp/contests/abc452/tasks/abc452_a
// 2026-04-04

'use strict';
const fs = require('fs');

function Main(input) {
    const args = input.trim().split(/\s+/);
    let cur = 0;
    const next = () => args[cur++];
    const nextNum = () => Number(next());
    const nextBigInt = () => BigInt(next());
    // const lines = input.replace(/\r/g, '').trim().split(/\n/);

    // 👇️ ロジックを記述 👇️

    const M = nextNum();
    const D = nextNum();

    if ((M === 1 && D === 7) || (M === D && [3, 5, 7, 9].includes(M))) {
        console.log('Yes');
    } else {
        console.log('No');
    }

    // 👆️ ロジックを記述 👆️
}

Main(
    require('fs').readFileSync(
        process.platform === 'linux' ? '/dev/stdin' : process.argv[2],
        'utf8',
    ),
);
```

与えられた$M$月$D$日が、1/7、3/3、5/5、7/7、9/9のいずれかであれば`"Yes"`、そうでなければ`"No"`を出力する問題です。上記のように、条件を考えて判別するか、下のコードのように該当する日付を配列として管理し、その中に$M$と$D$の組み合わせが含まれるか判定する、というやり方があります。

```javascript
    // 👇️ ロジックを記述 👇️

    const M = nextNum();
    const D = nextNum();
    const targets = ['1 7', '3 3', '5 5', '7 7', '9 9'];
    console.log(targets.includes(`${M} ${D}`) ? 'Yes' : 'No');

    // 👆️ ロジックを記述 👆️
```



## 🔠 [ABC452-B](https://atcoder.jp/contests/abc452/tasks/abc452_b)

```javascript
// ABC452
// https://atcoder.jp/contests/abc452/tasks/abc452_b
// 2026-04-04

'use strict';
const fs = require('fs');

function Main(input) {
    const args = input.trim().split(/\s+/);
    let cur = 0;
    const next = () => args[cur++];
    const nextNum = () => Number(next());
    const nextBigInt = () => BigInt(next());
    // const lines = input.replace(/\r/g, '').trim().split(/\n/);

    // 👇️ ロジックを記述 👇️

    const H = nextNum();
    const W = nextNum();

    for (let i = 0; i < H; i++) {
        if (i === 0 || i === H - 1) {
            console.log('#'.repeat(W));
        } else {
            console.log('#' + '.'.repeat(W - 2) + '#');
        }
    }

    // 👆️ ロジックを記述 👆️
}

Main(
    require('fs').readFileSync(
        process.platform === 'linux' ? '/dev/stdin' : process.argv[2],
        'utf8',
    ),
);
```

$H$行$W$列のグリッドを考えて、端に面した要素は`"#"`、それ以外は`"."`で埋める、という問題です。わたしは2次元配列を用意して、最終的に`.join("")`でくっつけて出力するということをしてしまったのですが、単純に1行毎に文字列で処理していけば良い問題でした。

コンテストを2週間スキップしたため、普段JSを書く機会がないわたしは、AもBも完全に勘が鈍ってしまって悲惨な結果となってしまいました。やはり毎週参加するつもりで日々訓練しないと駄目のようです。

## 🔠 [ABC452-C](https://atcoder.jp/contests/abc452/tasks/abc452_c)

```javascript
// ABC452
// https://atcoder.jp/contests/abc452/tasks/abc452_c
// 2026-04-04

'use strict';
const fs = require('fs');

function Main(input) {
    const args = input.trim().split(/\s+/);
    let cur = 0;
    const next = () => args[cur++];
    const nextNum = () => Number(next());
    const nextBigInt = () => BigInt(next());
    // const lines = input.replace(/\r/g, '').trim().split(/\n/);

    // 👇️ ロジックを記述 👇️

    const N = nextNum();
    const ribs = [];
    for (let i = 0; i < N; i++) {
        ribs.push({
            len: nextNum(),
            pos: nextNum(),
        });
    }

    const M = nextNum();
    const words = [];
    const conditionSet = new Set();

    for (let j = 0; j < M; j++) {
        const s = next();
        words.push(s);
        for (let k = 0; k < s.length; k++) {
            conditionSet.add(`${s.length}_${k + 1}_${s[k]}`);
        }
    }

    for (let j = 0; j < M; j++) {
        const spine = words[j];

        if (spine.length !== N) {
            console.log('No');
            continue;
        }

        let isPossible = true;
        for (let i = 0; i < N; i++) {
            const rib = ribs[i];
            const charAtSpine = spine[i];

            if (!conditionSet.has(`${rib.len}_${rib.pos}_${charAtSpine}`)) {
                isPossible = false;
                break;
            }
        }

        console.log(isPossible ? 'Yes' : 'No');
    }

    // 👆️ ロジックを記述 👆️
}

Main(
    require('fs').readFileSync(
        process.platform === 'linux' ? '/dev/stdin' : process.argv[2],
        'utf8',
    ),
);
```

制約を考えると、愚直にループを回すと処理が間に合わない問題です。それぞれの単語に対して、単語の長さ、どの文字が、何文字目か、ということを調べて管理する、ということを考えていて、実際方針としては合っていたのですが、コードに落とし込む力が不足していて解答できませんでした。

不参加だったABC450とABC451についても近日中にアップします。

---

# 📝 ABC452 Reflection (2026-04-04)

This was my first participation in two weeks. My rating has finally decreased. Personally, it felt like my worst performance ever. Since I haven't touched JavaScript or competitive programming at all for over two weeks, it was somewhat expected.

| **Rank** | **User** | **Score**         | **A**                                                        | **B**                                                        | **C** | **D** | **E** | **F** | **G** |
| -------- | -------- | ----------------- | ------------------------------------------------------------ | ------------------------------------------------------------ | ----- | ----- | ----- | ----- | ----- |
| 10009    | **Bice** | ** 300** (6)76:26 | [**100**](https://atcoder.jp/contests/abc452/submissions/74663254) (4)25:22 | [**200**](https://atcoder.jp/contests/abc452/submissions/74673023) (2)46:26 | (6)   | -     | -     | -     | -     |

I made four penalties on A and two on B. It was quite disastrous—reading the problem incorrectly, missing simple conditions, including unnecessary output, and failing to provide the answer in the required format.

## 🏆 Contest Results

| **Item**       | **Content**                                                  |
| -------------- | ------------------------------------------------------------ |
| Username       | [**Bice**](https://atcoder.jp/users/Bice)                    |
| Contest Name   | [AtCoder Beginner Contest 452](https://atcoder.jp/contests/abc452) |
| Rank           | [10009th](https://atcoder.jp/contests/abc452/standings?watching=Bice) / 12501 |
| Performance    | 129                                                          |
| Rating         | **232** → **219** (-13)                                      |
| **Rank/Grade** | **10th Kyu**                                                 |

## 📈 About the Rating

There's not much to say. Since my first participation performance was 134, I have set a new personal low.

I will review problems A, B, and C with reference to Gemini's advice (please check the links for problem statements, as I will refrain from posting them due to copyright).

## 🔠 [ABC452-A](https://atcoder.jp/contests/abc452/tasks/abc452_a)

```javascript
// ABC452
// [https://atcoder.jp/contests/abc452/tasks/abc452_a](https://atcoder.jp/contests/abc452/tasks/abc452_a)
// 2026-04-04

'use strict';
const fs = require('fs');

function Main(input) {
    const args = input.trim().split(/\s+/);
    let cur = 0;
    const next = () => args[cur++];
    const nextNum = () => Number(next());
    const nextBigInt = () => BigInt(next());
    // const lines = input.replace(/\r/g, '').trim().split(/\n/);

    // 👇️ Write logic here 👇️

    const M = nextNum();
    const D = nextNum();

    if ((M === 1 && D === 7) || (M === D && [3, 5, 7, 9].includes(M))) {
        console.log('Yes');
    } else {
        console.log('No');
    }

    // 👆️ Write logic here 👆️
}

Main(
    require('fs').readFileSync(
        process.platform === 'linux' ? '/dev/stdin' : process.argv[2],
        'utf8',
    ),
);
```

This problem asks to output `"Yes"` if the given month $M$ and day $D$ fall on 1/7, 3/3, 5/5, 7/7, or 9/9, and `"No"` otherwise. As shown above, one way is to evaluate the conditions directly, or you can manage the target dates in an array and check if the combination of $M$ and $D$ exists within it, as shown in the code below.

```javascript
    // 👇️ Write logic here 👇️

    const M = nextNum();
    const D = nextNum();
    const targets = ['1 7', '3 3', '5 5', '7 7', '9 9'];
    console.log(targets.includes(`${M} ${D}`) ? 'Yes' : 'No');

    // 👆️ Write logic here 👆️
```

## 🔠 [ABC452-B](https://atcoder.jp/contests/abc452/tasks/abc452_b)

```javascript
// ABC452
// [https://atcoder.jp/contests/abc452/tasks/abc452_b](https://atcoder.jp/contests/abc452/tasks/abc452_b)
// 2026-04-04

'use strict';
const fs = require('fs');

function Main(input) {
    const args = input.trim().split(/\s+/);
    let cur = 0;
    const next = () => args[cur++];
    const nextNum = () => Number(next());
    const nextBigInt = () => BigInt(next());
    // const lines = input.replace(/\r/g, '').trim().split(/\n/);

    // 👇️ Write logic here 👇️

    const H = nextNum();
    const W = nextNum();

    for (let i = 0; i < H; i++) {
        if (i === 0 || i === H - 1) {
            console.log('#'.repeat(W));
        } else {
            console.log('#' + '.'.repeat(W - 2) + '#');
        }
    }

    // 👆️ Write logic here 👆️
}

Main(
    require('fs').readFileSync(
        process.platform === 'linux' ? '/dev/stdin' : process.argv[2],
        'utf8',
    ),
);
```

The problem is to consider an $H \times W$ grid where elements on the edges are filled with `"#"` and everything else with `"."`. I ended up preparing a 2D array and joining it with `.join("")` for output, but it was a problem that could be handled simply by processing line by line as strings.

Having skipped two weeks of contests, and not usually having opportunities to write JS, I completely lost my edge on both A and B, resulting in a miserable outcome. It seems I must train daily with the intention of participating every week.

## 🔠 [ABC452-C](https://atcoder.jp/contests/abc452/tasks/abc452_c)

```javascript
// ABC452
// [https://atcoder.jp/contests/abc452/tasks/abc452_c](https://atcoder.jp/contests/abc452/tasks/abc452_c)
// 2026-04-04

'use strict';
const fs = require('fs');

function Main(input) {
    const args = input.trim().split(/\s+/);
    let cur = 0;
    const next = () => args[cur++];
    const nextNum = () => Number(next());
    const nextBigInt = () => BigInt(next());
    // const lines = input.replace(/\r/g, '').trim().split(/\n/);

    // 👇️ Write logic here 👇️

    const N = nextNum();
    const ribs = [];
    for (let i = 0; i < N; i++) {
        ribs.push({
            len: nextNum(),
            pos: nextNum(),
        });
    }

    const M = nextNum();
    const words = [];
    const conditionSet = new Set();

    for (let j = 0; j < M; j++) {
        const s = next();
        words.push(s);
        for (let k = 0; k < s.length; k++) {
            conditionSet.add(`${s.length}_${k + 1}_${s[k]}`);
        }
    }

    for (let j = 0; j < M; j++) {
        const spine = words[j];

        if (spine.length !== N) {
            console.log('No');
            continue;
        }

        let isPossible = true;
        for (let i = 0; i < N; i++) {
            const rib = ribs[i];
            const charAtSpine = spine[i];

            if (!conditionSet.has(`${rib.len}_${rib.pos}_${charAtSpine}`)) {
                isPossible = false;
                break;
            }
        }

        console.log(isPossible ? 'Yes' : 'No');
    }

    // 👆️ Write logic here 👆️
}

Main(
    require('fs').readFileSync(
        process.platform === 'linux' ? '/dev/stdin' : process.argv[2],
        'utf8',
    ),
);
```

Considering the constraints, this is a problem where brute-forcing the loops would exceed the time limit. I thought about managing each word by tracking its length, which character appears, and at which position. My strategy was actually correct, but I lacked the implementation skills to translate it into code and couldn't solve it.

I will also upload reflections for ABC450 and ABC451, which I did not participate in, shortly.