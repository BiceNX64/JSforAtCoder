**Scroll down for English**

# 📝 ABC442振り返り(2026-01-24)

AB2完でした。

| **順位** | **ユーザ** | **得点**         | **A**                                                        | **B**                                                        | **C** | **D** | **E** | **F** | **G** |
| -------- | ---------- | ---------------- | ------------------------------------------------------------ | ------------------------------------------------------------ | ----- | ----- | ----- | ----- | ----- |
| 8591     | **Bice**   | **300** (1)25:57 | [**100**](https://atcoder.jp/contests/abc442/submissions/72682618) (1)4:50 | [**200**](https://www.google.com/search?q=https://atcoder.jp/contests/abc442/submissions/72698589)20:57 | (2)   | -     | -     | -     | -     |

なかなかC問題が解けないです。今回も余計な出力でA問題でお手つきしてしまいました。

## 🏆 コンテスト成績

| **項目**           | **内容**                                                     |
| ------------------ | ------------------------------------------------------------ |
| **ユーザ名**       | [**Bice**](https://atcoder.jp/users/Bice)                    |
| **コンテスト名**   | [JPRSプログラミングコンテスト2026#1（AtCoder Beginner Contest 442）](https://atcoder.jp/contests/abc442) |
| **順位**           | [8591st](https://atcoder.jp/contests/abc442/standings?watching=Bice) / 11653 |
| **パフォーマンス** | 181                                                          |
| **レーティング**   | **157** → **159** (+2) Highest更新！                         |
| **段級位**         | **11 級** → **10 級**                                        |

## 📈 レーティングについて

かろうじてレーティング+2ということで非常に厳しい結果でした。パフォーマンスの181という数字も、3回目以降の参加で最も低いです。

A、B、Cの問題をGeminiのアドバイスを参考にレビューしていきます(問題文の掲載は著作権等の関係で控えさせていただきます。Linkから確認してください)。

## 🔠 [ABC442-A](https://atcoder.jp/contests/abc442/tasks/abc442_a)

```javascript
// ABC
// https://atcoder.jp/contests/abc442/tasks/abc442_a
// 2026-01-24

"use strict";
const fs = require('fs');

function Main(input) {
    const args = input.trim().split(/\s+/);
    let cur = 0;
    const next = () => args[cur++];
    const nextNum = () => Number(next());
    const nextBigInt = () => BigInt(next());
    // const lines = input.replace(/\r/g, '').trim().split(/\n/);

    // 👇️ ロジックを記述 👇️

    const S = next(); 
    let count = 0;

    for (const char of S) {
        if (char === "i" || char === "j") {
            count++;
        }
    }
    console.log(count);

    // 👆️ ロジックを記述 👆️
}

Main(require("fs").readFileSync(process.platform === 'linux' ? "/dev/stdin" : process.argv[2], "utf8"));
```

元々は下記のようにSを`split`し配列にしてfor文で処理していたのですが、たしかにGeminiが推奨するとおりStringに対して`for ... of`で1文字ずつ取り出すほうがこの場合は簡潔になります。

```javascript
    const S = next().split("");
    let count = 0;
    for (let i = 0; i < S.length; i++) {
        if (S[i] === "i" || S[i] === "j") {
            count++;
        }
    }
    console.log(count);
```

問題でお手つきをしてしまったのは、`S`の中身を確認するための出力を消し忘れたことでした。

## 🔠 [ABC442-B](https://atcoder.jp/contests/abc442/tasks/abc442_b)

```javascript
// ABC
// https://atcoder.jp/contests/abc442/tasks/abc442_b
// 2026-01-24

"use strict";
const fs = require('fs');

function Main(input) {
    const args = input.trim().split(/\s+/);
    let cur = 0;
    const next = () => args[cur++];
    const nextNum = () => Number(next());

    const Q = nextNum();
    let vol = 0;
    let playing = false;
    
    const ans = [];

    for (let i = 0; i < Q; i++) {
        const A = nextNum();
        
        if (A === 3) {
            playing = !playing;
        } else if (A === 1) {
            vol += 1;
        } else {
            if (vol > 0) {
                vol -= 1;
            }
        }

        ans.push(vol >= 3 && playing ? "Yes" : "No");
    }

    console.log(ans.join("\n"));
}

Main(require("fs").readFileSync(process.platform === 'linux' ? "/dev/stdin" : process.argv[2], "utf8"));
```

全体としてロジックに誤りなどはなかったのですが、Geminiのアドバイスとして

```javascript
if (A === 3) {
    if (playing) {
        playing = false;
    } else if (!playing) {
        playing = true;
    }
```

の部分は、下記のように簡潔に`false`と`true`をスイッチさせることができる、とのことでした。

```javascript
if (A === 3) {
            playing = !playing;javascript
        }
```

また、元々下記のように、for文のなかで`console.log()`をしていたのですが、いったん配列`ans`を用意して答えを貯めておいて最後に出力しないと、もし数が大きい場合処理時間で引っかかる可能性がある、と指摘がありました(前回も全く同じ指摘があったのですが完全に忘れていました)。

```javascript
for (let i = 0; i < Q; i++) {
    // ... 処理 ...
    console.log(vol >= 3 && playing ? "Yes" : "No") // 毎回出力している
}
```

## 🔠 [ABC442-C](https://atcoder.jp/contests/abc442/tasks/abc442_c)

```javascript
// ABC
// https://atcoder.jp/contests/abc442/tasks/abc442_c
// 2026-01-24

"use strict";
const fs = require('fs');

function Main(input) {
    const args = input.trim().split(/\s+/);
    let cur = 0;
    const next = () => args[cur++];
    const nextNum = () => Number(next());
    const nextBigInt = () => BigInt(next());
    // const lines = input.replace(/\r/g, '').trim().split(/\n/);

    // 👇️ ロジックを記述 👇️

    const N = nextNum();
    const M = nextNum();

    const counts = new Array(N + 1).fill(0);

    for (let i = 0; i < M; i++) {
        counts[nextNum()]++;
        counts[nextNum()]++;
    }

    let ans = [];

    for (let i = 1; i <= N; i++) {
        const c = N - counts[i] - 1;
        if (c < 3) {
            ans.push(0);
        } else {
            ans.push(c * (c - 1) * (c - 2) / 6);
        }
    }
    console.log(ans.join(" "));
    // 👆️ ロジックを記述 👆️
}

Main(require("fs").readFileSync(process.platform === 'linux' ? "/dev/stdin" : process.argv[2], "utf8"));
```

この問題については、テストケース3問は解けたものの提出結果はTLEが多く解けませんでした。原因はロジックを考える際に、具体的にどの人が査読できるか？までを求めてからその組み合わせを計算してしまったため、計算量が大幅に増えてしまいました。

考え方としては、例えばn番目の人と利害関係があるのは何人かを調べる→査読できる人数(cとする)=全員‐利害関係がある人‐1(自分)→cの組み合わせを調べる(combination)、でOKです。

```javascript
const counts = new Array(N + 1).fill(0);

for (let i = 0; i < M; i++) {
    counts[nextNum()]++;
    counts[nextNum()]++;
}
```

まず、配列`counts`をつくり`N+1`個のゼロで埋めます。この`counts`の1~N+1がそれぞれの人が利害関係を持つ人数のカウンターになります。次に、AとBを次々に取得します。たとえばA=1なら、counts[1]++となります。このときに、A=1に対して利害関係を持つ人を記録する必要はなく、あくまでA=1と利害関係がある人数のみをカウントして記録します。Bも同様で、これをMまでやります。

```javascript
let ans = [];
```

答えを貯める配列`ans`を用意し、

```javascript
for (let i = 1; i <= N; i++) {
    const c = N - counts[i] - 1;
    if (c < 3) {
        ans.push(0);
    } else {
        ans.push(c * (c - 1) * (c - 2) / 6);
    }
}
```

cが査読できる人の数で、全員(N)から、i番目の人と利害関係があるひととi番目本人(1人)を引きます。次はcによる条件分岐で、cが3より小さければ査読できる組み合わせはないため0となります。cが3以上の場合は、Combinationの公式どおりです(nCr)。このCombinationの計算に関しても、3名を選ぶことが決まっているのでシンプルに、`c * (c - 1) * (c - 2) / (3 * 2 * 1)`とすれば良かったのですが、頭が回らずネットで見つけた組み合わせの関数を適用して余計に時間をかけてしまいました。

今回のわたしの敗因としては3つです。

1. 問題の読解に時間がかかってしまった。
2. 解答を得るために必要のない処理を大量にしてしまった。
3. 高校数学で習った公式を使わずネットで調べた余計な関数を使ってしまった。

コンテスト終了後にXをみていると、簡単に解けているひとばかりでなかなか心が挫けそうですが、すぐに解決できる改善策もなく、とにかく地道に積み上げていくしかないのではないかと思います。

# 📝 ABC442 Review (2026-01-24)

I solved Problems A and B.

| **Rank** | **User** | **Score**        | **A**                                                        | **B**                                                        | **C** | **D** | **E** | **F** | **G** |
| -------- | -------- | ---------------- | ------------------------------------------------------------ | ------------------------------------------------------------ | ----- | ----- | ----- | ----- | ----- |
| 8591     | **Bice** | **300** (1)25:57 | [**100**](https://atcoder.jp/contests/abc442/submissions/72682618) (1)4:50 | [**200**](https://www.google.com/search?q=https://atcoder.jp/contests/abc442/submissions/72698589)20:57 | (2)   | -     | -     | -     | -     |

I'm still struggling to solve Problem C. This time, I also made a mistake on Problem A by forgetting to remove some debug output.

## 🏆 Contest Results

| **Item**         | **Content**                                                  |
| ---------------- | ------------------------------------------------------------ |
| **Username**     | [**Bice**](https://atcoder.jp/users/Bice)                    |
| **Contest Name** | [JPRS Programming Contest 2026#1 (AtCoder Beginner Contest 442)](https://atcoder.jp/contests/abc442) |
| **Rank**         | [8591st](https://atcoder.jp/contests/abc442/standings?watching=Bice) / 11653 |
| **Performance**  | 181                                                          |
| **Rating**       | **157** → **159** (+2) New Highest!                          |
| **Grade/Rank**   | **11 kyu** → **10 kyu**                                      |

## 📈 About the Rating

It was a very tough result, barely gaining +2 in rating. My performance score of 181 is the lowest I've had since my third participation.

I will review problems A, B, and C based on Gemini's advice (problem statements are omitted for copyright reasons; please check via the links).

## 🔠 [ABC442-A](https://atcoder.jp/contests/abc442/tasks/abc442_a)

```javascript
// ABC
// https://atcoder.jp/contests/abc442/tasks/abc442_a
// 2026-01-24

"use strict";
const fs = require('fs');

function Main(input) {
    const args = input.trim().split(/\s+/);
    let cur = 0;
    const next = () => args[cur++];
    // Logic below
    const S = next(); 
    let count = 0;

    // Directly iterate through characters in the string
    for (const char of S) {
        if (char === "i" || char === "j") {
            count++;
        }
    }
    console.log(count);
}

Main(require("fs").readFileSync(process.platform === 'linux' ? "/dev/stdin" : process.argv[2], "utf8"));
```

Initially, I used `split("")` to create an array and iterated through it with a standard for-loop. However, as Gemini recommended, using `for ... of` directly on the string is much more concise for this case.

```javascript
    const S = next().split("");
    let count = 0;
    for (let i = 0; i < S.length; i++) {
        if (S[i] === "i" || S[i] === "j") {
            count++;
        }
    }
    console.log(count);
```

The reason for my penalty (wrong submission) was forgetting to delete a `console.log` I used to verify the content of `S`.

## 🔠 [ABC442-B](https://atcoder.jp/contests/abc442/tasks/abc442_b)

```javascript
// ABC
// https://atcoder.jp/contests/abc442/tasks/abc442_b
// 2026-01-24

"use strict";
const fs = require('fs');

function Main(input) {
    const args = input.trim().split(/\s+/);
    let cur = 0;
    const nextNum = () => Number(args[cur++]);

    const Q = nextNum();
    let vol = 0;
    let playing = false;
    const ans = [];

    for (let i = 0; i < Q; i++) {
        const A = nextNum();
        
        if (A === 3) {
            playing = !playing;
        } else if (A === 1) {
            vol += 1;
        } else {
            if (vol > 0) vol -= 1;
        }

        ans.push(vol >= 3 && playing ? "Yes" : "No");
    }

    console.log(ans.join("\n"));
}

Main(require("fs").readFileSync(process.platform === 'linux' ? "/dev/stdin" : process.argv[2], "utf8"));
```

While my logic wasn't incorrect, Gemini suggested that this part:

```javascript
if (A === 3) {
    if (playing) {
        playing = false;
    } else if (!playing) {
        playing = true;
    }
}
```

could be simplified to toggle the boolean value like this:

```javascript
if (A === 3) {
    playing = !playing;
}
```

Additionally, I had been using `console.log()` inside the for-loop. Gemini pointed out that it's better to store results in an array like `ans` and output them all at once at the end to avoid TLE on large inputs (I had forgotten this exact advice from last time).

## 🔠 [ABC442-C](https://atcoder.jp/contests/abc442/tasks/abc442_c)

```javascript
// ABC
// https://atcoder.jp/contests/abc442/tasks/abc442_c
// 2026-01-24

"use strict";
const fs = require('fs');

function Main(input) {
    const args = input.trim().split(/\s+/);
    let cur = 0;
    const nextNum = () => Number(args[cur++]);

    const N = nextNum();
    const M = nextNum();

    const counts = new Array(N + 1).fill(0);

    for (let i = 0; i < M; i++) {
        counts[nextNum()]++;
        counts[nextNum()]++;
    }

    let ans = [];

    for (let i = 1; i <= N; i++) {
        const c = N - counts[i] - 1;
        if (c < 3) {
            ans.push(0);
        } else {
            ans.push(c * (c - 1) * (c - 2) / 6);
        }
    }
    console.log(ans.join(" "));
}

Main(require("fs").readFileSync(process.platform === 'linux' ? "/dev/stdin" : process.argv[2], "utf8"));
```

For this problem, I passed the three sample test cases but received many TLEs on the submission. This happened because my logic calculated specifically *which* people could review for each person before calculating combinations, which significantly increased the computational complexity.

The correct approach is to count how many people have a conflict of interest with the $n$-th person $\to$ let the number of reviewers be $c = (\text{Total}) - (\text{People with conflict}) - 1 (\text{Self}) \to$ calculate the combinations from $c$.

```javascript
const counts = new Array(N + 1).fill(0);

for (let i = 0; i < M; i++) {
    counts[nextNum()]++;
    counts[nextNum()]++;
}
```

First, create a `counts` array filled with $N+1$ zeros. This serves as a counter for the number of conflicts each person has. Then, process $A$ and $B$. If $A=1$, increment `counts[1]`. There's no need to record *who* has a conflict with $A=1$; just the count is sufficient. Do the same for $B$ until all $M$ relationships are processed.

```javascript
for (let i = 1; i <= N; i++) {
    const c = N - counts[i] - 1;
    if (c < 3) {
        ans.push(0);
    } else {
        ans.push(c * (c - 1) * (c - 2) / 6);
    }
}
```

$c$ is the number of potential reviewers. We subtract from the total $N$ the number of people with conflicts and the person themselves (1). Then, branch based on $c$: if $c < 3$, there are no combinations possible, so push 0. If $c \geq 3$, use the combination formula ($nC3$). Since we are specifically choosing 3 people, it's simply $c \times (c - 1) \times (c - 2) / (3 \times 2 \times 1)$. I over-complicated this by using a general combination function I found online instead of this simple math.

I identified three main reasons for my failure:

1. It took too long to interpret the problem correctly.
2. I performed a lot of unnecessary processing.
3. I used an external function instead of applying basic high school math formulas.

Seeing how easily others solved it on X (formerly Twitter) is a bit discouraging, but there's no magic fix. I just have to keep building my skills step by step.